extends ../../layouts/default.pug
include ../../mixins/box-head.pug 
include ../../mixins/product-layout.pug

block main 
    .container.my-3 
        .row 
            .col-12
                +box-head("kết quả tìm kiếm")
        if(product.length > 0)
            +product-grid(product)
        else 
            h1(class="text-center") Không có sản phẩm này
    